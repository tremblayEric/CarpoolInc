<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme" parent="FrameworkRoot.Theme" /> <!-- doesn't contain the pre-L code from the values/styles.xml -->

    <style name="Theme.MyTheme" parent="Theme.MyTheme.Base">

        <item name="android:navigationBarColor">#000</item>
        <item name="android:statusBarColor">?colorPrimaryDark</item>

        <item name="android:alertDialogTheme">@style/Theme.MyTheme.AlertDialog</item>
    </style>

    <style name="Theme.MyTheme.WithNavDrawer" parent="Theme.MyTheme">
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="Theme.MyTheme.AlertDialog" parent="android:Theme.Material.Light.Dialog.Alert">
        <item name="android:colorPrimary">@color/material_deep_teal_200</item>
        <item name="android:colorPrimaryDark">@color/material_deep_teal_500</item>
        <item name="android:colorAccent">@color/theme_accent_2</item>
    </style>

    <style name="Widget.MyTheme.HeaderBar.Spinner" parent="Widget.MyTheme.HeaderBar.Spinner.Base">
        <item name="android:theme">@style/ActionBarThemeOverlay</item>
    </style>

    <style name="TabIndicator">
        <item name="android:theme">@style/ActionBarThemeOverlay</item>
    </style>
</resources>
